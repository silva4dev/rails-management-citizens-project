<!DOCTYPE html>
<html lang="pt-br">
  <head>
    <title>OM30</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>
  <body>
    <div class="flex h-screen flex-col">
      <% if flash[:success] %>
        <p class="p-5 bg-green-100 text-green-600 font-medium rounded-lg inline-block text-center" id="notice"><%= flash[:success] %></p>
      <% end %>

      <% if flash[:error] %>
        <p class="p-5 bg-red-100 text-red-600 font-medium rounded-lg inline-block text-center" id="alert"><%= flash[:error] %></p>
      <% end %>

      <nav class="bg-white shadow-md p-5">
        <div class="flex items-center gap-2 justify-center">
          <%= link_to municipes_path do %>
            <%= image_tag "https://hipsters.jobs/files/pictures/logo_escrito.jpg",
            class: "w-[60px]" %>
          <% end %>
        </div>
      </nav>

      <main class="flex-1 p-16">
        <div class="container mx-auto mt-3 px-5">
          <%= yield %>
        </div>
      </main>

      <footer
        class="p-5 bg-white flex items-center justify-end"
        style="box-shadow: 0 -3px 4px -2px rgba(0, 0, 0, 0.2);"
      >
        <p class="mr-1 text-zinc-700">Desenvolvido com</p><svg
          xmlns="http://www.w3.org/2000/svg"
          fill="none"
          viewBox="0 0 24 24"
          stroke-width="1.5"
          stroke="currentColor"
          class="w-7 h-7 fill-red-500"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            d="M21 8.25c0-2.485-2.099-4.5-4.688-4.5-1.935 0-3.597 1.126-4.312 2.733-.715-1.607-2.377-2.733-4.313-2.733C5.1 3.75 3 5.765 3 8.25c0 7.22 9 12 9 12s9-4.78 9-12Z"
          />
        </svg><p class="ml-1">por
          <span class="font-bold">Lucas</span></p>
      </footer>
    </div>
  </body>
</html>
